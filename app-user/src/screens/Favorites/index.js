import React from 'react';
import { Text } from 'react-native';
import { Container } from '../../assets/styles/common';

export default () => {
    return (
        <Container>
            <Text>Favorites</Text>
        </Container>
    );
}